// cmd + shift + c
// cmd + alt + shift + c
import React from 'react';

class Baz extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div />
    );
  }
}
